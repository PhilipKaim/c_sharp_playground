{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/REST_client/REST_client.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}